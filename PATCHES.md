# PATCHES — adversarial review log（九步对攻产出，2026-09-12）

| ID | 优先级 | 身份 | 问题 | 根因 | 变更 | 验证 | 状态 |
|----|--------|------|------|------|------|------|------|
| PATCH-001 | P1 | 测试工程师 | 零测试，orphan/冲突逻辑回归无保护 | v1 快速发布 | GitHub Actions CI + demo 断言（nodes=3/edges=3/orphans=skill-gamma/conflicts=1） | 本地 node 实跑断言通过 ✅ | closed |
| PATCH-002 | P2 | 维护者 | `nameToIds` 死代码（重构残留） | 边匹配改版后未清理 | 删除 | cli.js 实跑 3 edges 不变 ✅ | closed |
| PATCH-003 | P3 | 反方 | orphan 定义只看 inbound，"只引用别人、无人引用自己"的 skill 可能被误读为死资产 | 指标单一 | 未改——报告已含 outbound 字段，README 语义明确，观察 issue 再定 | — | open |
| PATCH-004 | P3 | 极简主义者 | O(n²) 正则匹配，500+ skill 大库会慢 | 简单实现 | 未改——v1 规模内（<200 skills）可接受，留 TODO | — | open |

## 第二轮（加硬：真实大库实跑 + 语义查重，2026-09-12 下午）

| ID | 优先级 | 身份 | 问题 | 根因 | 变更 | 验证 | 状态 |
|----|--------|------|------|------|------|------|------|
| PATCH-005 | P1 | 架构师 | 只能发现"显式引用彼此"的冲突；两个 skill 语义重复但互不引用 = 结构性盲区 | 只有正则引用图 | 新增零依赖语义查重：CJK 二元组 + ASCII 词 + TF-IDF + 余弦；输出 Top-N 候选 | 真实库 463 skills：命中 hyperframes≈hyperframes-core(0.53)、copywriting≈marketing-cro(0.48) 等真近亲 ✅ | closed |
| PATCH-006 | P1 | 统计怀疑者 | 首版默认阈值 0.72 是拍脑袋值——真实库上 0 命中，指标形同失效 | 未在大库标定 | 默认阈值改为按真实库标定的 **0.45**，并**永远输出 Top-10 候选**（不依赖阈值猜对） | 463 库 0.72→0 命中、0.45→3 命中且经人工核对为真近亲 ✅ | closed |
| PATCH-007 | P2 | 测试工程师 | 语义模块无独立断言 | 新增功能 | CI 增加语义字段存在性断言 | 本地断言通过 ✅ | closed |
| PATCH-008 | P1 | 白队 | "真实大库实跑"此前只是承诺，无证据 | 无真实数据 | 在 463 skills / 469 目录真实库实跑并留痕（报告仅本地，不进仓——含内部 skill 名） | 4.3s 跑完：1371 边 / 73 孤儿 / 112 枢纽 / 24 冲突 / 覆盖率 463/469 ✅ | closed |
| PATCH-009 | P3 | 极简主义者 | 大库 O(n²) 语义对：463² ≈ 107k 次余弦，4.3s；1000+ 库会到 20s+ | 全对暴力 | 未改——当前实测可接受；>1500 skills 时再上倒排索引（已在 README 风险节标注） | — | open |

## 第三轮（发布前检测流程，2026-09-12 下午）

| ID | 优先级 | 身份 | 问题 | 根因 | 变更 | 验证 | 状态 |
|----|--------|------|------|------|------|------|------|
| PATCH-010 | **P0** | 统计怀疑者 | 语义打分把 `name+描述+正文` 混成**单一向量**：两个 skill 描述逐字相同却只得 **0.40**（低于阈值 0.45）→ **该功能存在的唯一理由（抓描述级重复）反而漏报** | 信号被无关正文稀释 | 拆成两路独立 TF-IDF（`description` / `body`）取 max，输出 `basis` 列标明命中来源 | demo 同描述对 **0.401 → 0.826** 正确入榜；真实库命中 3→6 对 ✅ | closed |
| PATCH-011 | P1 | 白队 | `top` 字段注释承诺"始终输出候选"，实现却对**已过滤**的 pairs 做 slice → 低于阈值时为空，文档与实现不符 | 实现与注释不一致 | `top` 改为排序后的全量非零对；`pairs` 保留阈值过滤 | demo 与真实库 `top` 均非空 ✅ | closed |
| PATCH-012 | P2 | 测试工程师 | CI 语义断言只检查"字段存在"，抓不到 PATCH-010/011 这类回归 | 断言过弱 | CI 加：`top` 非空 + 重复描述必须入榜 + score ≥ 0.6 + `basis === 'description'` | 本地 node 断言通过 ✅ | closed |
| PATCH-013 | P3 | 维护者 | 无 `.gitignore`，运行生成的 report.json/html 可能误入库 | 缺失 | 新增 `.gitignore`（忽略 out/report/demo-report 与 node_modules） | — | closed |
